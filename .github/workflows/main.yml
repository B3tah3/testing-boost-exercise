name: SSE CI/CD

on: [push]

jobs:
  style_check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: "Run style check"
        run: clang-format --dry-run --Werror src/*.cpp src/*.hpp tests/*.cpp tests/*.hpp
        
      
